# Lumio Context: Lesson 06

## Part 1 — Multi-Tab & Popup Management (formerly M31)

### Real-time collaboration in Lumio

Lumio uses WebSocket presence events to sync board state across sessions.
When User A adds a card, the server broadcasts a board_updated event and
all connected clients re-fetch or patch their local state.

### Auth state files

The fixture auth files at `tests/fixtures/auth/` are created by the global
setup script (M08). For multi-user tests you need two separate saved states:

- `user-a.json` — logged in as alice@lumio.test
- `user-b.json` — logged in as bob@lumio.test

If these don't exist, add a second user to the global setup.

### Key testids

| Element | data-testid |
|---------|-------------|
| Add card button | `add-card-button` |
| New card input | `new-card-input` |
| Kanban card | `kanban-card` |
| Presence avatar | `presence-avatar` |

### Context vs Page

| | BrowserContext | Page |
|---|---|---|
| Analogy | Browser profile | Browser tab |
| Cookies shared | Within same context | N/A |
| Auth state | Per context | Inherits from context |
| Create via | `browser.newContext()` | `context.newPage()` |

## Part 2 — WebSocket & SSE Testing (formerly M32)

### WebSocket in Lumio

Endpoint: `wss://localhost:3000/api/presence`

| Event | Direction | Payload |
|-------|-----------|---------|
| `user_joined` | Server to Client | `{ type, userId, name, boardId }` |
| `user_left` | Server to Client | `{ type, userId }` |
| `cursor_move` | Client to Server | `{ type, x, y }` |

### UI elements

| Element | data-testid | Trigger |
|---------|-------------|---------|
| Presence bar | `presence-indicator` | WS connected |
| User avatar | `presence-avatar` | user_joined received |
| Reconnect banner | `ws-reconnect-banner` | WS connection lost |

### Where to find this in the code

```
lumio/lib/presence/usePresence.ts      -> WS lifecycle hook
lumio/components/board/PresenceBar.tsx -> renders presence-indicator and avatars
```

## Part 3 — User Journey Simulation (formerly M33)

### What Lumio looks like at M33

By module 33, Lumio is a fully functional SaaS application with:

- **Auth** — email/password signup, email verification via token link, session cookies
- **Onboarding** — workspace creation form, member invite by email
- **Dashboard** — project list, task management (create, assign, comment)
- **Real-time** — WebSocket notifications for task assignment (`wss://localhost:3000/api/ws`)

This makes Lumio ideal for end-to-end journey testing: every feature connects to the next one.

### User journey route map

| Step | Route | Notes |
|------|-------|-------|
| Sign up | `/signup` | Creates unverified user |
| Verify email | `/verify-email?token={token}` | Token seeded in test DB; use `test-token-123` |
| Create workspace | `/onboarding/workspace` | Required before accessing dashboard |
| Invite member | `/onboarding/invite` | Optional; can be skipped in happy-path |
| Dashboard | `/dashboard` | Shows projects and assigned tasks |

### data-testid reference for M33

| Element | data-testid | Notes |
|---------|-------------|-------|
| Email field (signup) | `signup-email` | |
| Password field (signup) | `signup-password` | |
| Submit button (signup) | `signup-submit` | |
| Verification status | `verify-email-status` | Visible after token is accepted |
| Workspace name input | `workspace-name-input` | |
| Workspace submit | `workspace-submit` | |
| Create project button | `create-project-button` | Dashboard toolbar |
| Project name input | `project-name-input` | Inside create-project dialog |
| Project submit | `project-submit` | |
| Project card | `project-card` | One per project in list |
| Add task button | `add-task-button` | Inside project view |
| Task title input | `task-title-input` | |
| Task submit | `task-submit` | |
| Task card | `task-card` | One per task |
| Task assignee display | `task-assignee` | Shows assignee name on the card |
| Task assignee selector | `task-assignee-select` | Inside task detail panel |
| Task detail close | `task-detail-close` | Closes the detail side panel |

### Auth state files

Pre-authenticated session files live at `tests/fixtures/auth/`:

| File | User | Role |
|------|------|------|
| `user-a.json` | alice@lumio.test | Admin |
| `user-b.json` | bob@lumio.test | Member |
| `journey-user.json` | Written at runtime by the "first run" test | — |

These are generated by the global setup script (see M09). For the two-user tests in M33, both `user-a.json` and `user-b.json` must exist and correspond to users who share a workspace and project.

### Relevant source files

```
lumio/
└── app/
    ├── (auth)/
    │   ├── signup/page.tsx          -> signup form UI
    │   └── verify-email/page.tsx    -> reads ?token param, calls verify API
    ├── (onboarding)/
    │   └── onboarding/
    │       ├── workspace/page.tsx   -> workspace creation form
    │       └── invite/page.tsx      -> email invite form
    └── (protected)/
        └── dashboard/page.tsx       -> project list + task board
```

### WebSocket events relevant to assignment

When user A assigns a task to user B, Lumio emits a `task_assigned` event over the shared workspace WebSocket channel. User B's dashboard listens for this event and updates the task card in real time.

| Event | Direction | Payload |
|-------|-----------|---------|
| `task_assigned` | Server → Client | `{ taskId, assigneeId, assigneeName }` |
| `task_created` | Server → Client | `{ taskId, title, projectId }` |
